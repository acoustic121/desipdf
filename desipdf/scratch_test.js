const { spawn } = require('child_process');
const ytDlpPath = '/Users/amansingh/Documents/desipdf/desipdf/bin/yt-dlp' || 'yt-dlp'; // Need to find the local yt-dlp path or download it.
// Actually, let's just use `npx yt-dlp-exec` or just download the yt-dlp binary directly to test.
